🔝 Retour au [Sommaire](/SOMMAIRE.md)

# 1.3 Post-installation

## 1.3.1 Premier démarrage et configuration initiale

### Le premier démarrage

**Félicitations !** Votre système Debian est installé. Lors du premier démarrage, vous allez voir :

1. **Écran de démarrage GRUB** (si plusieurs systèmes)
2. **Messages de démarrage** du kernel Linux
3. **Services qui se lancent** (systemd)
4. **Écran de connexion** (login manager)

### Écran de connexion

#### Interface graphique (Desktop)

**Si vous avez installé un environnement de bureau :**

**GNOME Display Manager (GDM) :**
- Interface moderne et épurée
- Sélection d'utilisateur par clic
- Options de session en bas à droite
- Bouton d'arrêt/redémarrage en haut à droite

**Informations affichées :**
- **Liste des utilisateurs** créés pendant l'installation
- **Type de session** (GNOME, GNOME sur Wayland, etc.)
- **Clavier** et langue
- **État du réseau**

#### Interface en ligne de commande (Serveur)

**Si installation serveur (sans desktop) :**
```
Debian GNU/Linux 12 nom-machine tty1

nom-machine login: _
```

**Connexion :**
1. Tapez votre **nom d'utilisateur**
2. Appuyez sur **Entrée**
3. Tapez votre **mot de passe** (ne s'affiche pas - normal !)
4. Appuyez sur **Entrée**

### Configuration initiale automatique

#### Première connexion graphique

**Assistant de bienvenue (selon l'environnement) :**

**GNOME Initial Setup :**
1. **Langue et région** - Confirmation des paramètres
2. **Clavier** - Test et validation de la disposition
3. **Vie privée** - Paramètres de localisation et télémétrie
4. **Comptes en ligne** - Google, Microsoft, etc. (optionnel)
5. **Configuration terminée** - Accès au bureau

**Configuration recommandée pour débutants :**
- ✅ **Acceptez** les paramètres de langue détectés
- ✅ **Testez** le clavier (tapez quelques caractères spéciaux)
- ⚠️ **Vie privée** : Désactivez la localisation si pas nécessaire
- ❌ **Comptes en ligne** : À configurer plus tard si besoin

#### Vérifications essentielles

**À vérifier immédiatement :**

1. **Réseau fonctionnel**
   - L'icône réseau indique une connexion
   - Ouvrez un navigateur et testez une page web

2. **Son fonctionnel**
   - Cliquez sur l'icône volume
   - Testez la lecture d'un fichier audio

3. **Clavier correct**
   - Testez les caractères spéciaux : àéèç@#{[
   - Vérifiez les raccourcis Alt+Tab, Ctrl+C

4. **Affichage correct**
   - Résolution adaptée à votre écran
   - Pas de déformation ou flou

### Configuration de base du système

#### Paramètres système essentiels

**Accès aux paramètres :**
- **GNOME** : Menu Applications → Paramètres
- **KDE** : Menu → Configuration du système
- **XFCE** : Menu → Paramètres → Gestionnaire de paramètres

**Paramètres prioritaires à configurer :**

| Catégorie | Paramètre | Action recommandée |
|-----------|-----------|------------------|
| **Affichage** | Résolution | Sélectionner la résolution native |
| **Affichage** | Mise à l'échelle | Ajuster si texte trop petit/grand |
| **Région** | Format date/heure | Vérifier le format français |
| **Énergie** | Suspension | Configurer selon usage |
| **Réseau** | WiFi | Configurer si nécessaire |
| **Son** | Périphériques | Vérifier entrée/sortie |

#### Gestionnaire de fichiers

**Premier lancement du gestionnaire de fichiers :**
- **GNOME** : Files (Nautilus)
- **KDE** : Dolphin
- **XFCE** : Thunar

**Dossiers par défaut créés :**
```
/home/votre-nom/
├── Bureau/          (Desktop)
├── Documents/
├── Téléchargements/ (Downloads)
├── Images/          (Pictures)
├── Musique/         (Music)
├── Vidéos/          (Videos)
├── Public/
└── Modèles/         (Templates)
```

### Configuration réseau avancée

#### WiFi (si non configuré pendant l'installation)

**Connexion WiFi graphique :**
1. **Cliquez** sur l'icône réseau (barre de tâches)
2. **Sélectionnez** votre réseau WiFi
3. **Saisissez** la clé de sécurité
4. **Choisissez** "Se connecter automatiquement"
5. **Validez** avec "Se connecter"

**Résolution de problèmes WiFi courants :**

| Problème | Solution |
|----------|----------|
| **Réseau non visible** | Redémarrez le service : `sudo systemctl restart NetworkManager` |
| **Connexion échoue** | Vérifiez la clé WiFi, redémarrez le routeur |
| **Pilote manquant** | Installez les paquets firmware non-libres |
| **Signal faible** | Rapprochez-vous du routeur, vérifiez les interférences |

#### Pilotes propriétaires

**Vérification des pilotes manquants :**

**Méthode graphique (GNOME) :**
- Menu Applications → Logiciels
- Onglet "Mises à jour"
- Section "Pilotes supplémentaires"

**Méthode ligne de commande :**
```bash
# Vérifier les firmwares manquants
sudo dmesg | grep -i firmware

# Installer les firmwares non-libres si nécessaire
sudo apt update
sudo apt install firmware-linux-nonfree
```

**Pilotes courants nécessaires :**
- **WiFi** : firmware-iwlwifi, firmware-realtek
- **GPU NVIDIA** : nvidia-driver
- **GPU AMD** : firmware-amd-graphics
- **Bluetooth** : firmware-atheros, firmware-intel

## 1.3.2 Gestion des utilisateurs et groupes

### Comprendre les utilisateurs sous Linux

#### Types d'utilisateurs

**Utilisateur root (administrateur) :**
- **UID** : 0 (identifiant unique)
- **Privilèges** : Accès total au système
- **Usage** : Administration système uniquement
- **Sécurité** : ❌ Ne jamais utiliser pour les tâches quotidiennes

**Utilisateur standard :**
- **UID** : 1000+ (premier utilisateur = 1000)
- **Privilèges** : Limités à ses propres fichiers
- **Usage** : Travail quotidien, applications
- **Sécurité** : ✅ Recommandé pour usage normal

**Utilisateurs système :**
- **UID** : 1-999
- **Usage** : Services et démons système
- **Exemples** : www-data (Apache), mysql, postfix
- **Gestion** : Automatique, ne pas modifier

#### Informations stockées

**Fichier /etc/passwd :**
```
utilisateur:x:1000:1000:Nom Complet:/home/utilisateur:/bin/bash
```

**Signification des champs :**
1. **Nom d'utilisateur** : identifiant de connexion
2. **Mot de passe** : 'x' = stocké dans /etc/shadow
3. **UID** : identifiant numérique unique
4. **GID** : groupe principal (identique à UID par défaut)
5. **Nom complet** : nom réel de l'utilisateur
6. **Répertoire home** : dossier personnel (/home/nom)
7. **Shell** : interpréteur de commande (/bin/bash)

### Création d'utilisateurs

#### Méthode graphique

**GNOME (Paramètres) :**
1. **Ouvrez** Paramètres → Utilisateurs
2. **Déverrouillez** avec votre mot de passe
3. **Cliquez** "Ajouter un utilisateur"
4. **Choisissez** le type :
   - **Standard** : Utilisateur normal ✅
   - **Administrateur** : Accès sudo ⚠️
5. **Remplissez** les informations :
   - Nom complet
   - Nom d'utilisateur (pas d'espaces, minuscules)
   - Mot de passe sécurisé

**KDE (Paramètres système) :**
- Configuration du système → Gestion des utilisateurs
- Processus similaire avec interface KDE

#### Méthode ligne de commande

**Commande adduser (recommandée) :**
```bash
# Créer un nouvel utilisateur (interactif)
sudo adduser nouveau-utilisateur
```

**Ce qui se passe :**
1. **Création** du compte utilisateur
2. **Création** du répertoire /home/nouveau-utilisateur
3. **Copie** des fichiers de configuration par défaut
4. **Attribution** du shell par défaut
5. **Demande** du mot de passe et informations

**Commande useradd (avancée) :**
```bash
# Création manuelle complète
sudo useradd -m -s /bin/bash -c "Nom Complet" nouveau-utilisateur
sudo passwd nouveau-utilisateur
```

**Options importantes :**
- **-m** : Crée le répertoire home
- **-s** : Définit le shell (/bin/bash)
- **-c** : Commentaire (nom complet)
- **-G** : Groupes supplémentaires

### Comprendre les groupes

#### Qu'est-ce qu'un groupe ?

Un **groupe** est un ensemble d'utilisateurs partageant des **permissions communes**.

**Exemples de groupes système importants :**

| Groupe | Usage | Membres typiques |
|--------|-------|------------------|
| **sudo** | Administration système | Utilisateurs administrateurs |
| **adm** | Lecture des logs | Administrateurs |
| **users** | Utilisateurs standards | Tous les utilisateurs normaux |
| **audio** | Accès périphériques son | Utilisateurs desktop |
| **video** | Accès périphériques vidéo | Utilisateurs desktop |
| **cdrom** | Accès CD/DVD | Utilisateurs desktop |
| **plugdev** | Périphériques amovibles | Utilisateurs desktop |
| **netdev** | Configuration réseau | Utilisateurs WiFi |

#### Groupes par défaut d'un utilisateur desktop

**Groupes automatiquement attribués :**
```bash
# Voir les groupes d'un utilisateur
groups nom-utilisateur

# Résultat typique :
nom-utilisateur : nom-utilisateur cdrom floppy audio dip video plugdev users netdev
```

### Modification des utilisateurs

#### Changer le mot de passe

**Méthode graphique :**
- Paramètres → Utilisateurs
- Cliquez sur l'utilisateur
- "Changer le mot de passe"

**Méthode ligne de commande :**
```bash
# Changer son propre mot de passe
passwd

# Changer le mot de passe d'un autre utilisateur (admin)
sudo passwd nom-utilisateur
```

#### Modifier les informations utilisateur

**Changer le nom complet :**
```bash
sudo chfn nom-utilisateur
```

**Changer le shell par défaut :**
```bash
# Voir les shells disponibles
cat /etc/shells

# Changer le shell
sudo chsh -s /bin/zsh nom-utilisateur
```

**Ajouter à un groupe :**
```bash
# Ajouter utilisateur au groupe sudo
sudo usermod -a -G sudo nom-utilisateur

# Ajouter à plusieurs groupes
sudo usermod -a -G sudo,audio,video nom-utilisateur
```

**⚠️ Important :** L'option **-a** (append) est cruciale ! Sans elle, l'utilisateur sera retiré de tous ses autres groupes.

### Suppression d'utilisateurs

#### Suppression sécurisée

**Supprimer un utilisateur en gardant ses fichiers :**
```bash
sudo deluser nom-utilisateur
```

**Supprimer un utilisateur ET ses fichiers :**
```bash
sudo deluser --remove-home nom-utilisateur
```

**Supprimer complètement (utilisateur + groupe + fichiers) :**
```bash
sudo deluser --remove-all-files nom-utilisateur
```

#### Vérifications avant suppression

**À vérifier avant suppression :**
1. **Processus en cours** : `ps aux | grep nom-utilisateur`
2. **Fichiers importants** dans /home/nom-utilisateur
3. **Services** utilisant ce compte
4. **Cron tasks** de cet utilisateur : `sudo crontab -u nom-utilisateur -l`

## 1.3.3 Configuration de sudo

### Installation de sudo

#### Vérifier la présence de sudo

**Première étape : vérifier si sudo est installé**
```bash
which sudo
```

**Résultats possibles :**
- **Chemin affiché** (ex: /usr/bin/sudo) → ✅ sudo est installé
- **Aucun résultat** → ❌ sudo n'est pas installé

**Ou tester directement :**
```bash
sudo --version
```

#### Quand sudo n'est-il pas installé ?

**Installations Debian sans sudo :**
- **Installation serveur** minimale
- **Installation personnalisée** sans tâches desktop
- **Anciennes images** Debian
- **Installation netinst** avec sélection minimale

#### Installation de sudo

**Si sudo est absent, installation nécessaire :**

**1. Connexion en tant que root :**
```bash
su -
```
*Saisissez le mot de passe root défini pendant l'installation*

**2. Mise à jour des paquets :**
```bash
apt update
```

**3. Installation de sudo :**
```bash
apt install sudo
```

**4. Ajouter votre utilisateur au groupe sudo :**
```bash
usermod -a -G sudo nom-utilisateur
```
*Remplacez "nom-utilisateur" par votre nom d'utilisateur réel*

**5. Vérification de l'installation :**
```bash
which sudo
sudo --version
```

**6. Vérifier l'ajout au groupe :**
```bash
groups nom-utilisateur
```
*Vous devez voir "sudo" dans la liste des groupes*

**7. Sortir de la session root :**
```bash
exit
```

**8. IMPORTANT : Reconnexion nécessaire**
```bash
# Déconnectez-vous complètement et reconnectez-vous
# Ou redémarrez la session pour que les groupes soient pris en compte
```

**9. Test final :**
```bash
sudo whoami
```
*Doit afficher "root" si tout fonctionne*

#### Problème : mot de passe root oublié

**Si vous avez oublié le mot de passe root :**

**Méthode 1 : Mode recovery (recommandée)**
1. **Redémarrez** l'ordinateur
2. **Sélectionnez** "Advanced options" dans GRUB
3. **Choisissez** le mode recovery
4. **Sélectionnez** "root" dans le menu
5. **Montez** en écriture : `mount -o remount,rw /`
6. **Changez** le mot de passe : `passwd root`
7. **Redémarrez** : `reboot`

**Méthode 2 : Boot depuis Live USB**
1. **Bootez** sur une clé USB Debian Live
2. **Montez** votre partition système
3. **Chroot** dans votre système
4. **Changez** le mot de passe root
5. **Redémarrez** sur votre système

### Qu'est-ce que sudo ?

**sudo** = **S**ubstitute **U**ser an **DO** (Remplacer utilisateur et faire)

**Principe :**
- Permet d'**exécuter des commandes** avec les privilèges d'un autre utilisateur (généralement root)
- **Alternative sécurisée** à la connexion directe en root
- **Traçabilité** : toutes les actions sont loggées
- **Temporaire** : privilèges accordés pour une commande/session limitée

#### Avantages de sudo vs connexion root

| Aspect | Connexion root directe | sudo |
|--------|----------------------|------|
| **Sécurité** | ❌ Dangereux | ✅ Sécurisé |
| **Traçabilité** | ❌ Aucune | ✅ Logs détaillés |
| **Granularité** | ❌ Tout ou rien | ✅ Permissions spécifiques |
| **Accidentel** | ❌ Erreurs graves | ✅ Limité dans le temps |
| **Multi-utilisateur** | ❌ Partage mot de passe | ✅ Chacun son compte |

### Configuration de base de sudo

#### Fichier de configuration principal

**Fichier /etc/sudoers :**
- **Contient** toutes les règles sudo
- **Ne jamais éditer directement** avec un éditeur standard
- **Toujours utiliser** `sudo visudo` pour éviter les erreurs de syntaxe

#### Commande visudo

**Édition sécurisée :**
```bash
sudo visudo
```

**Avantages de visudo :**
- **Verrouillage** du fichier pendant l'édition
- **Vérification syntaxe** avant sauvegarde
- **Éditeur par défaut** (nano sur Debian)
- **Rollback automatique** si erreur

### Ajouter un utilisateur aux administrateurs

#### Méthode simple : groupe sudo

**Ajouter au groupe sudo (recommandé) :**
```bash
sudo usermod -a -G sudo nom-utilisateur
```

**Vérification :**
```bash
# Vérifier l'appartenance au groupe
groups nom-utilisateur

# L'utilisateur doit se déconnecter/reconnecter pour activation
```

#### Méthode avancée : règle personnalisée

**Ajout dans /etc/sudoers avec visudo :**
```bash
sudo visudo
```

**Ajouter la ligne :**
```
nom-utilisateur    ALL=(ALL:ALL) ALL
```

**Signification :**
- **nom-utilisateur** : L'utilisateur concerné
- **ALL** : Sur tous les hôtes
- **(ALL:ALL)** : Peut devenir n'importe quel utilisateur:groupe
- **ALL** : Peut exécuter toutes les commandes

### Configuration avancée de sudo

#### Règles granulaires

**Permettre seulement certaines commandes :**
```
nom-utilisateur ALL=(root) /usr/bin/apt, /usr/bin/systemctl
```

**Permettre sans mot de passe (dangereux) :**
```
nom-utilisateur ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart apache2
```

**Par groupe d'utilisateurs :**
```
%developers ALL=(www-data) /usr/bin/git, /bin/ls /var/www/
```

#### Configuration des délais

**Dans /etc/sudoers :**
```
# Durée de validité du cache de mot de passe (défaut: 15 min)
Defaults    timestamp_timeout=30

# Délai d'expiration d'une session sudo (défaut: 5 min)
Defaults    passwd_timeout=10
```

#### Logging et sécurité

**Activer le logging détaillé :**
```
# Dans /etc/sudoers
Defaults    log_input, log_output
Defaults    logfile="/var/log/sudo.log"
```

**Paramètres de sécurité :**
```
# Masquer les variables d'environnement
Defaults    env_reset

# Variables à conserver
Defaults    env_keep="LANG LANGUAGE LINGUAS LC_* _XKB_CHARSET"

# Requérir un mot de passe pour sudo
Defaults    !authenticate
```

### Utilisation pratique de sudo

#### Commandes de base

**Exécuter une commande en tant que root :**
```bash
sudo commande
```

**Ouvrir un shell root temporaire :**
```bash
sudo -i
# ou
sudo su -
```

**Exécuter en tant qu'autre utilisateur :**
```bash
sudo -u autre-utilisateur commande
```

**Vérifier les permissions sudo :**
```bash
sudo -l
```

#### Exemples courants

**Administration des paquets :**
```bash
sudo apt update
sudo apt upgrade
sudo apt install nom-paquet
```

**Gestion des services :**
```bash
sudo systemctl start nom-service
sudo systemctl enable nom-service
sudo systemctl status nom-service
```

**Édition de fichiers système :**
```bash
sudo nano /etc/hosts
sudo vim /etc/ssh/sshd_config
```

**Gestion des utilisateurs :**
```bash
sudo adduser nouveau-utilisateur
sudo passwd utilisateur
sudo usermod -a -G groupe utilisateur
```

### Résolution de problèmes sudo

#### Erreurs courantes

**"utilisateur n'est pas dans le fichier sudoers" :**
```bash
# Solution : ajouter l'utilisateur au groupe sudo
su -
usermod -a -G sudo nom-utilisateur
exit
# L'utilisateur doit se reconnecter
```

**"sudo: command not found" :**
```bash
# Installer sudo (en tant que root)
su -
apt update
apt install sudo
```

**Mot de passe incorrect répété :**
- Vérifiez la disposition du clavier
- Caps Lock activé ?
- Utilisez le bon mot de passe utilisateur (pas root)

#### Récupération d'accès

**Si sudo cassé, récupération avec root :**
1. **Redémarrez** en mode recovery
2. **Montez** le système en lecture-écriture
3. **Corrigez** /etc/sudoers avec visudo
4. **Redémarrez** normalement

**Ou via un autre utilisateur administrateur :**
```bash
# Si un autre admin existe
su autre-admin
sudo visudo
# Corrigez la configuration
```

## 1.3.4 Mise à jour du système

### Comprendre le système de mise à jour Debian

#### Sources de paquets

**Fichier /etc/apt/sources.list :**
```bash
# Dépôts principaux Debian 12 (Bookworm)
deb http://deb.debian.org/debian bookworm main
deb-src http://deb.debian.org/debian bookworm main

# Mises à jour de sécurité
deb http://security.debian.org/debian-security bookworm-security main
deb-src http://security.debian.org/debian-security bookworm-security main

# Mises à jour proposées
deb http://deb.debian.org/debian bookworm-updates main
deb-src http://deb.debian.org/debian bookworm-updates main
```

**Types de dépôts :**
- **main** : Paquets officiels libres ✅
- **contrib** : Libres mais dépendances non-libres ⚠️
- **non-free** : Paquets propriétaires ❌
- **security** : Correctifs de sécurité urgents 🔒
- **updates** : Corrections non-critiques 🔄

#### Différents types de mises à jour

**Mises à jour de sécurité :**
- **Priorité** : Critique
- **Installation** : Immédiate recommandée
- **Fréquence** : Dès publication
- **Source** : security.debian.org

**Mises à jour proposées (updates) :**
- **Priorité** : Important
- **Installation** : Régulière (hebdomadaire)
- **Fréquence** : Selon les besoins
- **Source** : bookworm-updates

**Backports :**
- **Priorité** : Optionnel
- **Installation** : Manuel uniquement
- **Usage** : Versions récentes de logiciels
- **Attention** : Peut casser la stabilité

### Commandes de mise à jour

#### Mise à jour manuelle (ligne de commande)

**Processus complet en 3 étapes :**

**1. Mise à jour de la liste des paquets :**
```bash
sudo apt update
```
**Ce que ça fait :**
- Télécharge les **listes** des paquets disponibles
- **Ne modifie rien** sur le système
- **Obligatoire** avant toute installation/mise à jour

**2. Mise à jour des paquets installés :**
```bash
sudo apt upgrade
```
**Ce que ça fait :**
- Met à jour **tous** les paquets installés
- **N'installe pas** de nouveaux paquets
- **Sûr** - ne casse pas les dépendances

**3. Mise à jour complète (optionnel) :**
```bash
sudo apt full-upgrade
```
**Ce que ça fait :**
- Met à jour **et installe/supprime** des paquets si nécessaire
- **Plus agressif** - peut changer des dépendances
- **Attention** - peut modifier le comportement

#### Commande tout-en-un

**Mise à jour complète automatique :**
```bash
sudo apt update && sudo apt upgrade
```

**Avec confirmation automatique :**
```bash
sudo apt update && sudo apt upgrade -y
```

#### Vérification avant mise à jour

**Voir les paquets à mettre à jour :**
```bash
apt list --upgradable
```

**Simuler une mise à jour (dry-run) :**
```bash
sudo apt upgrade --dry-run
```

**Voir les dépendances :**
```bash
apt-cache depends nom-paquet
```

### Mise à jour graphique

#### Gestionnaire de mises à jour GNOME

**Lancement :**
- Menu Applications → Logiciels
- Onglet "Mises à jour"

**Processus :**
1. **Recherche automatique** des mises à jour
2. **Liste** des paquets disponibles
3. **Sélection** (tout ou partie)
4. **Installation** avec demande de mot de passe
5. **Redémarrage** si nécessaire

#### Configuration des mises à jour automatiques

**GNOME Software :**
- Paramètres → Confidentialité → Diagnostics
- "Télécharger automatiquement les mises à jour"
- "Installer automatiquement les mises à jour de sécurité"

### Mises à jour automatiques

#### Configuration d'unattended-upgrades

**Installation :**
```bash
sudo apt install unattended-upgrades apt-listchanges
```

**Configuration automatique :**
```bash
sudo dpkg-reconfigure unattended-upgrades
```
- Répondre **Oui** pour activer

**Configuration manuelle (/etc/apt/apt.conf.d/50unattended-upgrades) :**
```bash
// Mise à jour automatique des paquets de sécurité
Unattended-Upgrade::Allowed-Origins {
    "${distro_id}:${distro_codename}-security";
    "${distro_id}ESMApps:${distro_codename}-apps-security";
};

// Redémarrage automatique si nécessaire (2h du matin)
Unattended-Upgrade::Automatic-Reboot "true";
Unattended-Upgrade::Automatic-Reboot-Time "02:00";

// Notifications par email
Unattended-Upgrade::Mail "admin@exemple.com";
```

#### Vérification du fonctionnement

**Status du service :**
```bash
sudo systemctl status unattended-upgrades
```

**Logs des mises à jour automatiques :**
```bash
sudo tail -f /var/log/unattended-upgrades/unattended-upgrades.log
```

**Test manuel :**
```bash
sudo unattended-upgrade --dry-run --debug
```

### Nettoyage du système

#### Suppression des paquets inutiles

**Supprimer les paquets orphelins :**
```bash
sudo apt autoremove
```

**Supprimer les fichiers de configuration obsolètes :**
```bash
sudo apt autopurge
```

#### Nettoyage du cache

**Nettoyer le cache APT :**
```bash
sudo apt autoclean    # Anciens paquets seulement
sudo apt clean        # Tout le cache
```

**Voir l'espace utilisé :**
```bash
du -sh /var/cache/apt/archives/
```

#### Nettoyage complet recommandé

**Routine de nettoyage mensuelle :**
```bash
sudo apt update
sudo apt upgrade
sudo apt autoremove
sudo apt autoclean
```

### Gestion des problèmes de mise à jour

#### Problèmes courants

**Dépendances cassées :**
```bash
sudo apt --fix-broken install
sudo dpkg --configure -a
```

**Paquet bloqué :**
```bash
sudo apt-mark hold nom-paquet    # Bloquer
sudo apt-mark unhold nom-paquet  # Débloquer
```

**Espace disque insuffisant :**
```bash
# Libérer de l'espace
sudo apt clean
sudo apt autoremove
df -h    # Vérifier l'espace
```

#### Restoration en cas de problème

**Downgrade d'un paquet (dangereux) :**
```bash
sudo apt install nom-paquet=version-precedente
```

**Restauration complète :**
- Utiliser une sauvegarde système
- Boot sur live USB pour réparation
- Réinstallation si nécessaire

### Bonnes pratiques de mise à jour

#### Fréquence recommandée

**Mises à jour de sécurité :** ✅ **Immédiatement**
**Mises à jour régulières :** ✅ **Hebdomadaire**
**Nettoyage système :** ✅ **Mensuel**

#### Avant une mise à jour importante

**Checklist de sécurité :**
1. ✅ **Sauvegarde** des données importantes
2. ✅ **Note** des paquets installés : `dpkg --get-selections > paquets.txt`
3. ✅ **Test** en environnement virtuel si possible
4. ✅ **Planning** - éviter les heures critiques
5. ✅ **Documentation** des modifications personnalisées

#### Surveillance post-mise à jour

**À vérifier après mise à jour :**
- **Services** fonctionnent : `sudo systemctl status`
- **Logs** sans erreurs : `sudo journalctl -xe`
- **Applications** se lancent correctement
- **Réseau** fonctionne
- **Sécurité** firewall actif

Votre système Debian est maintenant correctement configuré et sécurisé pour une utilisation quotidienne !

⏭️
