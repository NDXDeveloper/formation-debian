🔝 Retour au [Sommaire](/SOMMAIRE.md)

# 2.5 Bureautique et productivité

## Introduction

Ce module vous guide dans l'installation et la configuration des outils essentiels pour la bureautique et la productivité sous Debian. Nous couvrirons les applications de bureau courantes, les navigateurs web, les clients de messagerie, et les outils de développement.

## 2.5.1 Suite LibreOffice

### Qu'est-ce que LibreOffice ?

LibreOffice est une suite bureautique complète et gratuite, équivalente à Microsoft Office. Elle comprend :
- **Writer** : traitement de texte (équivalent à Word)
- **Calc** : tableur (équivalent à Excel)
- **Impress** : présentation (équivalent à PowerPoint)
- **Draw** : dessin vectoriel
- **Base** : base de données
- **Math** : éditeur d'équations

### Installation de LibreOffice

LibreOffice est généralement pré-installé sur Debian avec environnement de bureau. Pour vérifier ou installer :

```bash
# Vérifier si LibreOffice est installé
dpkg -l | grep libreoffice

# Installer la suite complète
sudo apt update
sudo apt install libreoffice

# Installer uniquement les composants essentiels
sudo apt install libreoffice-writer libreoffice-calc libreoffice-impress
```

### Installation des packs de langues

Pour une interface en français :

```bash
# Pack langue française
sudo apt install libreoffice-l10n-fr

# Dictionnaires français
sudo apt install hunspell-fr myspell-fr

# Thesaurus français
sudo apt install mythes-fr
```

### Configuration initiale

1. **Lancez LibreOffice** depuis le menu applications
2. **Première configuration** :
   - Choisir la langue d'interface
   - Configurer les informations utilisateur
   - Sélectionner le format de documents par défaut

3. **Optimisations recommandées** :
   - Aller dans `Outils` → `Options`
   - **Mémoire** : Augmenter la mémoire pour les objets (64 MB)
   - **Sauvegarde** : Activer la sauvegarde automatique (10 minutes)
   - **Formats** : Définir les formats par défaut

### Utilisation de base

#### Writer (Traitement de texte)
```
Raccourcis essentiels :
- Ctrl+N : Nouveau document
- Ctrl+O : Ouvrir
- Ctrl+S : Enregistrer
- Ctrl+B : Gras
- Ctrl+I : Italique
- Ctrl+U : Souligné
- F7 : Correcteur orthographique
```

#### Calc (Tableur)
```
Fonctions de base :
- =SOMME(A1:A10) : Somme d'une plage
- =MOYENNE(A1:A10) : Moyenne
- =MAX(A1:A10) : Valeur maximale
- =MIN(A1:A10) : Valeur minimale
```

## 2.5.2 Navigateurs web et extensions

### Firefox (navigateur par défaut)

Firefox est le navigateur par défaut de Debian. Pour l'optimiser :

```bash
# Mise à jour Firefox
sudo apt update
sudo apt upgrade firefox-esr

# Installation de Firefox standard (plus récent)
sudo apt install firefox
```

### Installation d'autres navigateurs

#### Google Chrome
```bash
# Télécharger et installer Chrome
wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
sudo apt update
sudo apt install google-chrome-stable
```

#### Chromium (version open source)
```bash
sudo apt install chromium
```

### Extensions Firefox recommandées

1. **uBlock Origin** : Bloqueur de publicités
2. **Privacy Badger** : Protection contre le tracking
3. **LastPass/Bitwarden** : Gestionnaire de mots de passe
4. **Dark Reader** : Mode sombre pour tous les sites

### Configuration de sécurité Firefox

1. Dans Firefox, aller à `about:config`
2. Paramètres recommandés :
   - `privacy.trackingprotection.enabled` = true
   - `network.cookie.cookieBehavior` = 1
   - `dom.security.https_only_mode` = true

## 2.5.3 Clients de messagerie

### Thunderbird (client email)

Thunderbird est le client email recommandé pour Debian :

```bash
# Installation
sudo apt install thunderbird

# Pack langue française
sudo apt install thunderbird-l10n-fr
```

### Configuration d'un compte email

1. **Lancer Thunderbird**
2. **Configuration automatique** :
   - Saisir nom, email et mot de passe
   - Thunderbird détecte automatiquement les paramètres
3. **Configuration manuelle** si nécessaire :
   - Serveur IMAP/POP3 entrant
   - Serveur SMTP sortant
   - Ports et chiffrement

### Paramètres courants des fournisseurs

#### Gmail
```
IMAP entrant :
- Serveur : imap.gmail.com
- Port : 993 (SSL)

SMTP sortant :
- Serveur : smtp.gmail.com
- Port : 587 (STARTTLS)
```

#### Outlook/Hotmail
```
IMAP entrant :
- Serveur : outlook.office365.com
- Port : 993 (SSL)

SMTP sortant :
- Serveur : smtp.office365.com
- Port : 587 (STARTTLS)
```

### Clients alternatifs

#### Evolution (client Gnome)
```bash
sudo apt install evolution
```

#### Claws Mail (léger)
```bash
sudo apt install claws-mail
```

## 2.5.4 Outils de développement (IDE, éditeurs)

### Éditeurs de texte

#### Gedit (simple)
```bash
sudo apt install gedit
```

#### Nano (terminal)
```bash
# Généralement pré-installé
nano mon_fichier.txt
```

#### Vim (avancé)
```bash
sudo apt install vim

# Configuration de base dans ~/.vimrc
echo "set number
set autoindent
set tabstop=4
set shiftwidth=4
syntax on" > ~/.vimrc
```

### IDE (Environnements de développement)

#### Code::Blocks (C/C++)
```bash
sudo apt install codeblocks
```

#### Eclipse (Java)
```bash
sudo apt install eclipse
```

#### NetBeans
```bash
# Installation via snap
sudo apt install snapd
sudo snap install netbeans --classic
```

### Outils de développement essentiels

#### Git (contrôle de version)
```bash
sudo apt install git

# Configuration initiale
git config --global user.name "Votre Nom"
git config --global user.email "votre@email.com"
```

#### Compilateurs et interpréteurs
```bash
# Compilateur C/C++
sudo apt install build-essential

# Python (généralement pré-installé)
sudo apt install python3 python3-pip

# Node.js
sudo apt install nodejs npm

# Java
sudo apt install default-jdk
```

## 2.5.5 Docker Desktop et VS Code

### Installation de VS Code

Visual Studio Code est un éditeur de code très populaire :

```bash
# Méthode 1 : Dépôt officiel Microsoft
wget -qO- https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > packages.microsoft.gpg
sudo install -o root -g root -m 644 packages.microsoft.gpg /etc/apt/trusted.gpg.d/
echo "deb [arch=amd64,arm64,armhf signed-by=/etc/apt/trusted.gpg.d/packages.microsoft.gpg] https://packages.microsoft.com/repos/code stable main" | sudo tee /etc/apt/sources.list.d/vscode.list

sudo apt update
sudo apt install code

# Méthode 2 : Via snap
sudo snap install code --classic
```

### Configuration de VS Code

#### Extensions recommandées pour débutants :
1. **French Language Pack** : Interface en français
2. **GitLens** : Amélioration Git
3. **Prettier** : Formatage automatique du code
4. **Live Server** : Serveur web local pour développement
5. **Material Icon Theme** : Icônes pour les fichiers

#### Installation d'extensions :
```bash
# Via ligne de commande
code --install-extension ms-ceintl.vscode-language-pack-fr
code --install-extension eamodio.gitlens
code --install-extension esbenp.prettier-vscode
```

### Installation de Docker

Docker permet de créer et gérer des conteneurs :

```bash
# Installation des dépendances
sudo apt install apt-transport-https ca-certificates curl gnupg lsb-release

# Ajout de la clé GPG Docker
curl -fsSL https://download.docker.com/linux/debian/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Ajout du dépôt Docker
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/debian $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Installation
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io

# Ajout de l'utilisateur au groupe docker
sudo usermod -aG docker $USER
```

### Configuration post-installation Docker

```bash
# Démarrage automatique
sudo systemctl enable docker
sudo systemctl start docker

# Test d'installation
docker run hello-world

# Installation de Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

## Conseils et bonnes pratiques

### Maintenance des applications

```bash
# Mise à jour régulière
sudo apt update && sudo apt upgrade

# Nettoyage des paquets obsolètes
sudo apt autoremove
sudo apt autoclean
```

### Sauvegarde des configurations

```bash
# Créer un dossier de sauvegarde
mkdir ~/backup-config

# Sauvegarder les configurations importantes
cp ~/.vimrc ~/backup-config/
cp ~/.gitconfig ~/backup-config/
cp -r ~/.config/Code ~/backup-config/vscode-config
```

### Optimisation des performances

1. **Fermer les applications inutilisées**
2. **Limiter les extensions de navigateur**
3. **Configurer les mises à jour automatiques**
4. **Surveiller l'utilisation mémoire** avec `htop`

## Dépannage courant

### LibreOffice lent au démarrage
```bash
# Réinitialiser le profil utilisateur
rm -rf ~/.config/libreoffice
```

### VS Code ne se lance pas
```bash
# Vérifier les permissions
sudo chown -R $USER:$USER ~/.config/Code
```

### Docker : permission denied
```bash
# Vérifier que l'utilisateur est dans le groupe docker
groups $USER

# Se déconnecter et reconnecter si nécessaire
# ou utiliser newgrp docker
```

## Conclusion

Vous avez maintenant un environnement de bureautique et de productivité complet sous Debian. Les applications installées couvrent :
- **Bureautique** : LibreOffice pour tous vos documents
- **Navigation** : Firefox et alternatives sécurisées
- **Communication** : Thunderbird pour vos emails
- **Développement** : VS Code et Docker pour la programmation

Dans le lab suivant, nous pratiquerons l'installation et la configuration de ces outils dans un environnement de développement complet.


### Ressources pour aller plus loin

- [Documentation officielle Debian](https://www.debian.org/doc/)
- [VS Code Documentation](https://code.visualstudio.com/docs)
- [Docker Documentation](https://docs.docker.com/)
- [Git Tutorial](https://git-scm.com/docs/gittutorial)
- [LibreOffice Help](https://help.libreoffice.org/)

**Votre environnement de développement Debian est maintenant prêt pour de grands projets !** 🚀


⏭️
