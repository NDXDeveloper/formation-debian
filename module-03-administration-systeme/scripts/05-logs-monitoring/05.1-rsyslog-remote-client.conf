#
# Nom         : 05.1-rsyslog-remote-client.conf
# Module      : 3 — Administration système
# Section     : 3.5.1 — rsyslog vs journald
# Source      : module-03-administration-systeme/05.1-rsyslog-vs-journald.md
# Description : Configuration rsyslog client pour envoi des logs vers un serveur
#               distant (RELP/TCP/TLS).
# Testé sur   : Debian 13 (Trixie)
# Licence     : CC BY 4.0 — Formation Debian (Nicolas DEOUX)
#
# Envoi via UDP (rapide mais non fiable — perte possible de messages)
*.*     @logserver.example.com:514

# Envoi via TCP (fiable — les messages sont retransmis en cas d'erreur)
*.*     @@logserver.example.com:514

# Envoi via TCP avec TLS (chiffré et fiable)
# Nécessite le module omrelp ou imtcp avec TLS
action(type="omfwd"
    target="logserver.example.com"
    port="514"
    protocol="tcp"
    StreamDriver="gtls"
    StreamDriverMode="1"
    StreamDriverAuthMode="x509/name")

# File d'attente sur disque (protection contre les pertes en cas
# d'indisponibilité du serveur distant)
action(type="omfwd"
    target="logserver.example.com"
    port="514"
    protocol="tcp"
    queue.type="LinkedList"
    queue.filename="fwd_logserver"
    queue.maxDiskSpace="1g"
    queue.saveOnShutdown="on"
    action.resumeRetryCount="-1"
    action.resumeInterval="30")
