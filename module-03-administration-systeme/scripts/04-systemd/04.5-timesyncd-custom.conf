#
# Nom         : 04.5-timesyncd-custom.conf
# Module      : 3 — Administration système
# Section     : 3.4.5 — systemd-networkd et systemd-resolved
# Source      : module-03-administration-systeme/04.5-systemd-network-resolved.md
# Description : Configuration des serveurs NTP via systemd-timesyncd.
# Testé sur   : Debian 13 (Trixie)
# Licence     : CC BY 4.0 — Formation Debian (Nicolas DEOUX)
#
[Time]
# Serveurs NTP principaux
NTP=ntp.example.com ntp2.example.com

# Serveurs de secours
FallbackNTP=0.debian.pool.ntp.org 1.debian.pool.ntp.org 2.debian.pool.ntp.org

# Intervalle de polling (secondes) — min et max
PollIntervalMinSec=32
PollIntervalMaxSec=2048

# Sauvegarder l'horodatage pour accélérer la synchronisation au prochain boot
SaveIntervalSec=60

# Décalage maximum accepté pour un ajustement progressif
# Au-delà, l'horloge est ajustée brutalement (step)
RootDistanceMaxSec=5
