# =============================================================================
# Module 19 — Architectures de référence
# Section 19.1.1 — Configuration poste développeur Debian
# Fichier : ~/.tmux.conf — productivité multiplexeur
# Licence : CC BY 4.0
# =============================================================================
# Config tmux orientée DevOps : préfixe Ctrl+a (vs Ctrl+b par défaut),
# panneaux numérotés à partir de 1, splits dans le répertoire courant,
# navigation Alt+flèches sans préfixe, historique 50k lignes, support
# souris (scroll/redimensionnement), True Color (256 + Tc).
#
# Rechargement : prefix + r
# =============================================================================

# Préfixe plus accessible (Ctrl+a au lieu de Ctrl+b)
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Numérotation des fenêtres et panneaux à partir de 1
set -g base-index 1
setw -g pane-base-index 1

# Séparation intuitive des panneaux
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Navigation entre panneaux avec Alt+flèches (sans préfixe)
bind -n M-Left  select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up    select-pane -U
bind -n M-Down  select-pane -D

# Historique étendu
set -g history-limit 50000

# Support souris (utile pour le scroll et le redimensionnement)
set -g mouse on

# Couleurs 256 et True Color
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*256col*:Tc"

# Rechargement de la configuration
bind r source-file ~/.tmux.conf \; display "Configuration rechargée"
